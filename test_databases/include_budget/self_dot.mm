$( Dot-spelled self-reference: literal identity treats it as a second file,
   re-includes it, and fails on the first redeclaration - exactly the
   reference implementations' contour.  Run from the repository root. $)
$[ ./test_databases/include_budget/self_dot.mm $]
$c wff $.
