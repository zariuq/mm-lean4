$( Same-spelling self-reference: the literal-identity mirror modes skip it
   exactly as the references do.  Run from the repository root. $)
$[ test_databases/include_budget/self_lit.mm $]
$c wff $.
